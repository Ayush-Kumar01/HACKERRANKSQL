SELECT ROUND(LAT_N,4)
FROM
(SELECT LAT_N, ROW_NUMBER() OVER (ORDER BY LAT_N DESC) AS DESC_LAT,
           ROW_NUMBER() OVER (ORDER BY LAT_N ASC) AS ASC_LAT
FROM STATION) AS A
WHERE ASC_LAT IN (DESC_LAT,DESC_LAT+1,DESC_LAT-1);
